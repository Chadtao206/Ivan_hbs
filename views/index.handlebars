<h2>WELCOME TO HBS!</h2>
<div class="container">
    <div class="row">
        {{#each data as |person|}}

        <div class="col-3">
            {{> card person}}
        </div>
        {{/each}}
    </div>
  </div>